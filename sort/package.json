{
  "name": "sort",
  "version": "1.0.0",
  "description": "`tsc --init` edit \"outDir\" and \"rootDir\" in tsconfig.json",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "start:build": "tsc -w",
    "start:run": "nodemon build/index.js",
    "start": "concurrently yarn:start:*"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "dependencies": {
    "concurrently": "^7.1.0",
    "nodemon": "^2.0.16"
  }
}
